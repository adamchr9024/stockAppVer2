import { ElementRef, Renderer2, RendererFactory2 } from '@angular/core';
import { PercentDirective } from './percent.directive';

describe('PercentDirective', () => {
  it('should create an instance', () => {
    let element: ElementRef = new ElementRef(HTMLTableCellElement);
    //let renderer:Renderer2 = RendererFactory2.;
    //const directive = new PercentDirective(element, renderer);
    //expect(directive).toBeTruthy();
  });
});
